rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{roomId} {
      allow read, write: if roomId.matches('^[0-9]{4}$');

      match /{document=**} {
        allow read, write: if roomId.matches('^[0-9]{4}$');
      }
    }
  }
}
